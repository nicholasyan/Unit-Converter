{
    "manifest_version": 2,

    "name": "Metric to Imperial",
    "description": "This extension automatically lists the metric unit equivalent next to any imperial units it finds on a page (and vice versa).",
    "version": "1.0",

    "options_page": "options.html",

    "permissions": [
        "activeTab",
        "storage"
    ],

    "content_scripts": [
        {
            "matches": ["<all_urls>"],
            "js": ["convert.js"],
            "css": ["convert.css"]
        }
    ]
}